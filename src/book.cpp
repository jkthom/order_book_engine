#include "book.h"

std::vector<Trade> OrderBook::add_order(const Order& o) {
    // stub: no matching yet
    return {};
}

bool OrderBook::cancel_order(uint64_t order_id) {
    // stub: nothing to cancel
    return false;
}
